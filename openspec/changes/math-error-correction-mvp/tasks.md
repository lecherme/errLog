## 1. Local Development Foundation

- [ ] 1.1 Set up docker-compose orchestrating three containers (Next.js, Python PDF Processing Service, self-hosted PostgreSQL) on an internal Docker network for local development only — no public ingress yet; verify all three start and reach each other by service name
- [ ] 1.2 Configure PostgreSQL in its own container with a dedicated named Docker volume, separate from the Next.js and Python service containers; verify the volume survives `docker compose down`/rebuild of the other two containers
- [ ] 1.3 Implement the persistence schema for User, Identity, Child, WorksheetArchive, GradingSession, ErrorQuestion, CorrectionWorksheet (D4/D7) using standard, portable PostgreSQL (no non-standard extensions); verify migrations apply cleanly to a fresh instance
- [ ] 1.4 Add a local-dev-only seed script inserting one allowlisted User/Identity/Child row for manual end-to-end testing; explicitly not a production auth path — verify the seeded identity can be used to drive the workflow manually ahead of real Auth (Group 5) being built

## 2. Storage Core (OSS)

- [ ] 2.1 Configure a private OSS bucket (D10): no public-read/public-write, versioning enabled; verify bucket ACL and versioning settings
- [ ] 2.2 Implement StorageService issuance of short-lived, scope-limited STS credentials (multipart upload) and presigned URLs (simple upload) for browser direct upload (D9); verify an issued credential cannot list, delete, or access objects outside its granted key
- [ ] 2.3 Implement server-side upload verification (object existence, size, content-type, checksum) gating Asset availability (D9); verify a browser-reported "upload success" without server verification does not produce a usable Asset
- [ ] 2.4 Implement server-generated, unique, unpredictable object keys for original assets, with re-upload creating a new Asset rather than overwriting; verify a repeated upload does not replace the existing object
- [ ] 2.5 Implement StorageService's signed GET URL issuance capability for private assets; in the local walking skeleton, gate it with an explicit dev identity / caller-context ownership check (using the Group 1.4 seeded identity) — not yet a full authenticated session; verify a request without any caller context is rejected and a request for an asset owned by a different identity is rejected. (Re-verified against real authenticated User + ownership in 5.5 — do not reimplement signed-URL logic there.)
- [ ] 2.6 Verify original PDF and grading-photo assets remain accessible and byte-identical after rebuilding/redeploying the Next.js container (worksheet-archive spec/grading-workflow spec/D3); record the verification

(OSS cross-account/cross-region backup replication is in Group 10 — Data Protection — not here, since it hardens recovery rather than enabling core upload/download function.)

## 3. PDF Processing Service Core

- [ ] 3.1 Scaffold the Python PDF Processing Service (PyMuPDF) as an independent Docker container implementing page-render, bbox-crop, and worksheet-assembly endpoints; verify the container starts and responds to a health check
- [ ] 3.2 Define the `PdfProcessingPort` interface in the Next.js Domain/Application layer and an Infrastructure HTTP adapter implementing it (D11); verify Domain/Application code has no import of an HTTP client or PyMuPDF-specific type
- [ ] 3.3 Implement least-privilege OSS access for the Python service — short-lived scoped credentials, or Next.js-mediated file transfer (D11); verify the service never holds a long-lived OSS AccessKey/SecretKey
- [ ] 3.4 Implement basic malformed/corrupt-PDF error handling and temporary-file cleanup in the service; verify a corrupt PDF input returns a clear error and leaves no orphaned temp files
- [ ] 3.5 Implement page-registration and region-mapping in the Python PDF Processing Service (D11/D16): given a PDF page render and a grading photo, estimate a homography via OpenCV feature matching (e.g. ORB + RANSAC); map a given PDF-space bbox into photo space with a **configurable** context margin (not a hardcoded magic constant); return the mapped region plus a reliability signal (e.g. inlier count/reprojection error). Validate against a representative sample set covering: clear/well-aligned photos, photos with meaningful perspective/skew, insufficiently-cropped photos (missing page overlap), blurry photos, and feature-poor photos (heavy occlusion by marks, sparse printed content); use these real samples to establish and configure the reliability threshold that decides "可靠映射 vs 回退整照片" — do not fabricate a threshold value now; the context margin's size must also be validated against these samples, not hardcoded without traceability. Verify: (a) the threshold and margin are configured from actual sample results, not invented numbers; (b) whenever the threshold has not yet been established, is not met, or the mapping result is otherwise anomalous, the endpoint reports unreliable/fallback rather than returning a bad mapping silently; (c) a well-aligned pair produces a plausible mapped region. (This task's mapping runs only after a specific question's clean-question region has already been confirmed — it is the D16 confirmed-box → photo-evidence-region mapping. The distinct page-level, pre-confirmation reliability signal D20 uses to gate pre-highlight is implemented separately in 3.8; the two are not the same execution point and must not be conflated.)

- [ ] 3.6 Implement PDF-type detection (D19) entirely within the Python PDF Processing Service (D11) — PDF structure/text-layer extraction, page-image analysis, font/vector-object inspection, and per-page consistency checks all live here; Next.js only triggers the task, persists/reads the resulting `PdfTypeDetectionAttempt[]`, and renders the UI prompt. Model: each detection or re-detection creates a new attempt (never deletes or overwrites a prior one); a single attempt's only legal status transitions are `pending → completed` and `pending → failed`, both terminal — once terminal, an attempt's fields (`detectedInputType`, resolution timestamp, `failureReason`) are never modified; a retry after `failed` creates a new attempt rather than reopening the old one. Current effective judgment SHALL be taken from the most-recently-created attempt only: `completed` → use its `detectedInputType`; `pending` or `failed` → `unknown`; no attempt → `unknown` — this SHALL NOT skip a newer pending/failed attempt to fall back on an older `completed` attempt's result. Only output `digital_native` when evidence meets the calibrated-and-frozen threshold for the active `detectionVersion` (fail-closed); output `unknown` whenever evidence is insufficient, contradictory, mixed, or below threshold. Detection is asynchronous, durable/retryable, idempotent under duplicate triggers, and never leaves an attempt permanently stuck in `pending` after a crash/restart (recovers via retry/reconciliation creating a new attempt if needed; no specific queue product mandated). Downstream code SHALL treat a `pending`/`failed`/no-attempt current judgment as `unknown` and SHALL NOT enter any `digital_native`-exclusive product branch otherwise. Verify: (a) a repeated trigger for the same upload does not create a duplicate in-flight attempt or lose existing history; (b) a crash mid-detection recovers rather than an attempt sticking in `pending` forever; (c) detection never blocks upload completion; (d) a `digital_native` current judgment is never produced unless the *most recent* attempt is `completed` with that value — a newer `pending`/`failed` attempt correctly overrides an older `completed`/`digital_native` attempt's judgment; (e) re-detection appends a new attempt rather than mutating a prior terminal one, and prior attempts remain queryable; (f) a `failed` attempt is retried via a new attempt, never by mutating the failed one back to `pending`.

- [ ] 3.7 Implement `QuestionBoxNumberAssociation` (D20) as a deterministic step in the Python PDF Processing Service (D11) — not an AI Task Contract, no third-party Provider call. Input: a PDF page's text-layer/structural information + the question boxes produced by whole-page segmentation's `full_page` mode for that page. Output per box: `{boxId, questionNumberCandidate?, associationStatus: reliable|ambiguous|unavailable, associationVersion, evidence?, confidence?}`. digital_native inputs prioritize text-layer content/position; scanned/mixed/unknown inputs MAY use local OCR/structural signals best-effort with no formal guarantee. Build a human-labeled evaluation sample set split into an independent **calibration subset** (used to determine and freeze the reliability threshold and the active `associationVersion`) and a separate **held-out subset** (used to evaluate final association quality) — no sample serves both roles; MAY reuse Dataset B's (6.2) PDF/box assets, plus a required per-box question-number annotation layer (no-visible-number/irregular-numbering samples kept as legitimate `unavailable`/`ambiguous` ground truth, never excluded). Freeze the threshold and `associationVersion` before running the held-out subset; held-out results SHALL NOT trigger re-tuning within the same `associationVersion` — an adjustment to the algorithm or threshold SHALL produce a new `associationVersion` and repeat calibration → freeze → held-out. On the held-out subset, record: false-association rate, reliable-association precision, coverage, and ambiguous/unavailable rate. Only a `reliable` **and** strictly one-to-one association may drive pre-highlight; duplicate-number/no-number/many-to-one/one-to-many/below-threshold cases are `ambiguous`/`unavailable` and SHALL NOT drive pre-highlight. The association result is a candidate signal only — the sole authoritative source of a saved ErrorQuestion's identity remains the user-confirmed box set. Verify: (a) clear digital_native text-layer numbering produces `reliable` associations meeting the frozen threshold on the held-out subset; (b) duplicate/ambiguous/no-number samples are never marked `reliable`; (c) no third-party Provider is ever called; (d) held-out metrics are computed on samples never used in calibration; (e) a threshold/algorithm change produces a new `associationVersion` rather than mutating results under the old version.
- [ ] 3.8 Implement a page-level, pre-confirmation registration/layout-consistency reliability signal in the Python PDF Processing Service (D11/D20) — distinct from and independent of 3.5's confirmed-box → photo-evidence-region mapping (D16), which only runs after a specific question's region has already been confirmed; do not conflate the two into the same execution point. This signal MUST be obtainable before any single question box in the session has been confirmed. Input: the grading photo and PDF page render that D14 has already confirmed correspond to each other (no per-question region required as input). Output: a page-level registration/layout-consistency reliability score (may reuse 3.5's underlying registration algorithm, applied at the page level rather than per-question). Used by D20 to decide whether pre-highlight may be shown at all for a grading session, and by D20's repair-dispatch spatial-compatibility check (whether a known question-number's expected position is spatially consistent with a parent's click). Calibrate the reliability threshold against a representative sample set covering clear/well-aligned, skewed, insufficiently-cropped, blurry, and feature-poor photo/PDF pairs — do not invent a threshold value now. Verify: (a) the signal can be computed and returns a result before any question box in the session has been confirmed; (b) it does not require or depend on 3.5/D16's per-question mapping having run; (c) when the signal is below the calibrated threshold, pre-highlight is disabled for the session but `full_page` segmentation and plain click-selection remain fully available, unblocked; (d) a clearly mismatched photo/PDF pair (e.g. different print run/reflow) produces a low reliability score and no pre-highlight is shown for that session; (e) 3.5's own confirmed-box-to-evidence-region mapping (D16) is unaffected and continues to run only after per-question confirmation, independent of this task's execution.

(Request/job identity, idempotent retry, timeout, and "service unavailable → explicit failure" handling are in Group 11 — Service Robustness — since the happy path above is what the walking skeleton needs first.)

## 4. Core Upload, Page Confirmation & Pipeline Foundations

Uses the seeded identity from 1.4 and the core pieces from Groups 2–3. This Group builds real, permanent product components — PDF/photo upload, archive management, the D14 page-confirmation UI, PDF-type detection UI, and the crop→worksheet-assembly pipeline — but it does **not** provide any user-visible normal or exception entry point for confirming error questions or creating an `ErrorQuestion`. That entry point (normal: `full_page` segmentation + click-select, per D20; exception: click-to-repair) is built entirely in Group 7 and requires Group 6's contract adapters (6.4/6.5) and Group 3's deterministic steps (3.7/3.8). A build that ships Groups 1–5 without Group 6–7 has no way for a parent to create an ErrorQuestion for either path — this is intentional, not a gap to route around with 4.3's fixture. 4.1/4.2/4.4–4.7/4.9–4.11 cover the **PDF-backed path** (grading-workflow spec); 4.8 covers the separate **no-PDF degraded path** (grading-workflow spec/D15). 4.3 is a dev-only fixture (see below), not a product component of either path. These are two distinct product paths, not sequential or mutually required global steps — a GradingSession follows exactly one of them depending on whether a PDF was selected.

- [ ] 4.1 Implement PDF upload + naming (worksheet-archive spec) using Storage Core, plus the archive list view (name, upload time, empty state); verify a PDF can be uploaded, named, and appears in the list, AND verify a submission with an empty name is rejected with a prompt to fill it in
- [ ] 4.2 Implement grading-photo upload (one or more photos) + archive selection for the PDF-backed path (grading-workflow spec — selecting a PDF archive is optional per that spec; this task covers the case where a PDF is selected, see 4.8 for the no-PDF case), creating a GradingSession linking the photo(s) and chosen PDF; verify multi-photo upload works and single-photo is a valid case
- [ ] 4.3 Implement a dev-only seed/fixture mechanism providing confirmed question-set data for downstream pipeline verification (4.5–4.7 and other Groups needing ErrorQuestion-shaped data before Group 6/7 land): fixed sample PDF page, fixed confirmed pageId, fixed confirmed question region/bbox (and any other downstream fields needed, e.g. placeholder question numbers), injected via a test helper / seed script / dev-only internal mechanism — analogous to 1.4's dev-seed identity. This mechanism SHALL NOT implement any browser-reachable UI for editing/entering question numbers or regions; it SHALL NOT be reachable by a parent through any product route; it SHALL be disabled/unreachable in a production build (verified by 13.5, mirroring 13.4's check on 1.4). Fixture-created data SHALL be clearly marked as dev/test data (or held in an isolated dev/test database/schema), SHALL NOT be mixed into real User/Child/WorksheetArchive/GradingSession/ErrorQuestion data, and SHALL be cleanable after test runs. Verify: (a) the fixture produces a confirmed question region usable by 4.5's crop step without any UI interaction; (b) no code path exposes this mechanism through the app's normal navigation or API surface reachable by an authenticated parent session; (c) invoking it against a production configuration fails or is not present; (d) fixture-created rows are tagged/isolated as dev/test data and are never returned by any real-user query path; (e) fixture data can be cleaned up after a test run without touching real data.
- [ ] 4.4 Implement the PDF page confirmation UI for the PDF-backed path (D14/grading-workflow spec — this task assumes a PDF was selected in 4.2): for a single-page PDF, auto-select that page; for a multi-page PDF, provide a full manual page-picker (thumbnail/page-list) requiring an explicit confirm action before a page is treated as confirmed — this is D14's own permanent no-reliable-candidate exception path, not a temporary skeleton, and remains part of the shipped product after 7.4 adds AI pre-selection on top of it. Persist the confirmed page immediately per D6 on confirm. This task does NOT include any per-question region/bounding-box UI — question-level identification and localization are built entirely in Group 7 (D20); no such UI exists anywhere in Group 4. Verify: (a) a single-page PDF skips the picker; (b) a multi-page pick is not persisted as confirmed until the explicit confirm action; (c) the confirmed page survives a page refresh; (d) this task produces no question-level UI of any kind.
- [ ] 4.5 Implement clean-question extraction (crop) via `PdfProcessingPort` using the confirmed region info produced by 4.3's dev-only fixture (PDF-backed path only — see 4.8 for the no-PDF path, which has no clean-question extraction); verify the extracted image contains no handwriting or grading marks, AND positively verify that formulas, geometric figures, and tables within the cropped region are fully preserved (correction-worksheet spec)
- [ ] 4.6 Implement correction-worksheet PDF assembly from the extracted question images (PDF-backed path only), using the default answer space (type classification not yet wired in); verify a printable PDF is produced with one answer space per extracted question
- [ ] 4.7 Run and record a **pipeline-only** walkthrough of 4.1–4.6 using one real sample worksheet and 4.3's seeded confirmed region (not a claim of a complete user-facing journey — the real end-to-end user journey is verified only once Group 7's normal entry exists, see 7.7); verify the crop→worksheet-assembly pipeline produces a correct correction worksheet from a real PDF.
- [ ] 4.8 Implement the no-PDF grading-task creation path (grading-workflow spec/D15): allow creating a GradingSession without selecting a WorksheetArchive (4.2 covers the PDF-backed case; this task covers the no-PDF case); skip the page-correspondence step (D14/4.4) entirely for this path. This path has no manual-only region/segmentation UI, and does NOT reuse 4.3 as a parent-facing entry point — 4.3 is a dev-only fixture, not a product UI, for either path. The click-to-select segmentation UI is only available once wired with AI in 7.5/6.5, not before. Verify a GradingSession can be created without a PDF, does not trigger any page-selection UI, and no product-facing route exists for creating ErrorQuestions in this path until 7.5/6.5 land.
- [ ] 4.9 Implement answer-evidence context-region generation for the PDF-backed path (D16/grading-workflow spec), using the confirmed clean-question region (however established — 4.3's dev-only fixture for pipeline validation, or the real product flow's parent-confirmed box set from Group 7 once it lands) and confirmed page mapping (D14): call the Group 3 registration/mapping endpoint (3.5) to produce either a candidate context-region result or a full-photo-fallback result; verify a successful mapping produces a candidate-region result, a failed/low-reliability/insufficient mapping produces a full-photo-fallback result instead, and this generation step does not itself persist anything as confirmed (persistence happens in 4.10) nor block error-question saving.
- [ ] 4.10 Implement the answer-evidence-region preview, boundary-nudge, and confirmation UI (D16/grading-workflow spec): display the candidate context region generated in 4.9 (or the full-photo fallback) alongside the confirmed clean-question region; allow the parent to drag the candidate region's boundary for a small adjustment (no from-scratch draw tool); persist the region as a confirmed business fact only once the parent explicitly confirms it as part of confirming that error question — before this confirmation it remains a candidate result, not a confirmed fact usable by downstream steps. Verify: (a) an unconfirmed candidate/fallback is never treated as confirmed or used by downstream steps; (b) once confirmed, no automatic re-registration, re-mapping, or other automatic recompute silently overwrites the confirmed region (this is not an "AI recompute" — page registration is deterministic image processing, not AI — but the same non-overwrite guarantee applies); (c) the full-photo-fallback state offers no region-editing, only confirmation of the whole photo as the answer evidence.

- [ ] 4.11 Wire the current PDF-type detection judgment (3.6) into the worksheet-archive UI (worksheet-archive spec/D19): show only the minimal, non-assertive, non-technical prompts based on the current effective judgment (most-recent-attempt rule from 3.6) — current judgment `pending` → '正在分析文档，自动切题暂按尽力而为处理'; current judgment `unknown` (failed, no attempt, or completed-but-unknown) → '暂时无法确认文件类型，自动切题将按尽力而为处理'; current judgment `scanned` → '系统检测该文件可能为扫描版，自动切题将按尽力而为处理'; current judgment `digital_native` → no extra prompt. Never surface `detectedInputType`/`detectionVersion`/`confidence` directly to the parent, and never phrase any prompt as a confirmed fact. scanned/unknown/failed archives remain fully selectable for grading (best-effort notice only, not blocked). Verify: (a) each current-judgment state renders its specified prompt and no other; (b) no raw technical field is shown; (c) selecting a scanned/unknown/failed archive for grading is not blocked; (d) when a newer pending/failed attempt supersedes an older completed/digital_native attempt, the UI immediately reflects the newer (unknown/analyzing) state, not the stale digital_native one.

## 5. Auth, Access Control & Child Profile

- [ ] 5.1 Implement the AuthService abstraction with the User/Identity model (D2); verify Domain code references only `User.id`, never a provider-specific identifier
- [ ] 5.2 Integrate SMS OTP via 阿里云 PNVS 短信认证 (D12); verify a real phone number can complete an OTP login end to end
- [ ] 5.3 Execute the Email OTP Selection Gate (D12): test account provisioning and sender/domain verification for 阿里云 DirectMail and 腾讯云 SES, and send OTP to qq.com/163.com/126.com plus one international mailbox, recording delivery time and spam-folder outcome; record the decision and integrate the selected provider behind AuthService
- [ ] 5.4 Implement first-login auto-provisioning of the User record for an allowlisted email/phone, replacing the 1.4 dev-seed path for real usage (per corrected Migration Plan — no manual DB seeding in production). Child records are NOT auto-created here — first-login Child creation goes through the child-profile onboarding flow (5.7); verify the first successful OTP login for an allowlisted identifier creates the expected User record and, when the User has no Child yet, is routed to child-profile onboarding rather than silently receiving a Child record.
- [ ] 5.5 Implement access-control middleware rejecting unauthenticated requests to every protected route/API (access-control spec); re-verify the Group 2.5 signed GET URL issuance against a real authenticated User + ownership authorization, reusing its existing logic rather than reimplementing it; verify unauthenticated access is blocked and redirected with no business data returned, and that 2.5's dev-identity placeholder check is fully replaced by real session-based authorization
- [ ] 5.6 Verify the access-control spec's resource-access boundary directly against the server/API (not merely UI visibility) — this task verifies access-control's own boundary only, and SHALL NOT reimplement the authentication logic already covered by 5.5 or the Child-ownership binding logic already covered by 5.8–5.10: (a) an authenticated user can reach a representative set of their own account's resources (worksheet archives, grading sessions, error questions) where each resource's own other access preconditions (per its owning capability's spec) are already satisfied; (b) an authenticated user's direct API request for a WorksheetArchive, GradingSession, or ErrorQuestion belonging to a different User account is rejected by the server, even when the request is otherwise well-formed and authenticated. Verify both (a) and (b) via direct server/API calls, not just by checking that a UI element is hidden or absent
- [ ] 5.7 Implement the child-profile onboarding flow (child-profile spec): after login, if the User has no Child, guide them to create the first Child; block entry to grading-workflow/question-bank/correction-worksheet until at least one Child exists; verify a User with no Child is routed to onboarding and cannot reach those features until a Child is created.
- [ ] 5.8 Implement the "current Child" pointer (`currentChildId`) on User: persisted server-side, defaults to the most recently used Child on login, unchanged unless the user explicitly creates or switches a Child (child-profile spec); verify the pointer survives logout/login and a fresh device/session, is not reset by unrelated actions, always resolves to a Child owned by that User, and any attempt — including a direct/forged request — to set it to a Child belonging to another User is rejected.
- [ ] 5.9 Implement create-additional-child and switch-current-child UI (simple list; no independent login/delete/merge/invite) (child-profile spec); verify a second Child can be created and becomes current, a multi-child user sees a switch entry, switching updates `currentChildId` without touching any existing GradingSession/ErrorQuestion/CorrectionWorksheet record, creating the very first Child immediately sets it as `currentChildId` without a separate switch step, and switching `currentChildId` to a Child not owned by the requesting User is rejected server-side even when attempted directly against the API.
- [ ] 5.10 Implement Child-binding invariants across GradingSession/ErrorQuestion/CorrectionWorksheet (D4/child-profile spec), each following its own rule rather than a shared one: a GradingSession binds childId from `currentChildId` at creation time and never changes it afterward; an ErrorQuestion's childId is inherited entirely from its GradingSession and never reads `currentChildId`, at creation or at submit time; a CorrectionWorksheet's childId is derived from the set of ErrorQuestions selected at generation time (requiring they share one childId) and never reads `currentChildId`, even when `currentChildId` has since changed. The server SHALL re-validate, at GradingSession-creation time, that the childId being bound actually belongs to the requesting User rather than trusting a client-submitted value. Verify: (a) switching `currentChildId` mid-session does not change an already-created GradingSession's childId; (b) an ErrorQuestion committed after such a mid-session switch still resolves to the GradingSession's original childId; (c) generating a CorrectionWorksheet from ErrorQuestions belonging to two different Children is rejected; (d) generating a CorrectionWorksheet from ErrorQuestions that all belong to one Child succeeds and is attributed to that Child even if `currentChildId` has since switched away; (e) a GradingSession-creation request carrying a forged or mismatched childId not owned by the requesting User is rejected by the server rather than trusted.
- [ ] 5.11 Verify WorksheetArchive remains User-level shared and is not scoped to any Child (D4/child-profile spec): create GradingSessions for two different Children both referencing the same WorksheetArchive/PDF, and verify both succeed without the archive becoming exclusive to either Child.

## 6. AI Integration & Selection Gate

AI Integration owns the Task Contract interfaces and model adapter only. It does **not** implement the human-review UI or answer-space sizing logic — those are built in Group 7 (identification/localization UI, net-new per D20) and Group 9 (answer-space sizing), not duplicated here. Group 4 provides only page-confirmation UI (4.4) and pipeline-proving fixtures (4.3) — no question-level human-review or fallback UI exists there to extend.

- [ ] 6.1 Define the six AI Task Contract interfaces — error-question recognition, region localization, whole-page question segmentation (with discriminated `full_page`/`locality_retry` request-response shapes per D20 — see 6.2/6.5), knowledge-tag labeling, question-type classification, error-cause analysis (D5) — independent of any model SDK; verify Domain/workflow code depends only on these interfaces
- [ ] 6.2 Build four separate, fixed, reusable evaluation datasets per D13/D19, using **human-labeled** ground truth throughout (the D19 detector's own output is never used to bucket any of these — avoids circular validation):
  - **Dataset A — PDF-type detector**: human-labeled `digital_native`/`scanned`/`mixed-or-ambiguous` PDFs (`mixed-or-ambiguous` is a human-labelable file-characteristic category; `unknown` is the detector's own output state and is never used as a ground-truth label); split into a calibration subset (sets/freezes a `detectionVersion`'s thresholds) and an independent held-out subset (evaluates final classification, produces the confusion matrix) — no sample serves both roles.
  - **Dataset B — whole-page question segmentation (`full_page`, PDF-render mode)**: `digital_native` subset split into calibration + held-out (formal Gate, per D19's frozen-threshold and no-crash-any-qualifying-sample rules); `scanned` subset (exploratory, same metric definitions as digital_native, no calibration/held-out split required); `mixed-or-ambiguous` subset — MAY reuse Dataset A's PDF assets, but SHALL NOT be assumed to already carry segmentation-ready labels; before inclusion in Dataset B, each reused PDF SHALL be separately labeled with the whole-page-segmentation-specific ground truth it needs: the full list of true questions on the page, each one's correct boundary, per-question content-completeness judgment basis, false-positive determination basis, duplicate/over-segmentation determination basis, merge-error determination basis, and the per-PDF crash-determination basis. This is a substantive relabeling from the prior region-localization-style ground truth (single target number + single boundary), not a rename. `QuestionBoxNumberAssociation` (3.7) MAY reuse this dataset's PDF/box assets but requires an additional per-box human question-number annotation layer of its own (no-visible-number/irregular-numbering samples kept as legitimate ground truth, not excluded).
  - **Dataset C — whole-page question segmentation (`full_page`, grading-photo mode)**: grading-photo samples (exploratory, per P1(4)/D15); sample unit is one photo; each photo SHALL be human-labeled with every true question present and its correct boundary before it can be used to compute miss rate, inaccurate-box rate, false-positive/spurious-box rate, duplicate/over-segmentation rate, or merge-error rate.
  - **Dataset D — cross-page / out-of-scope stress set**: collected separately; never enters any formal Gate's pass/fail judgment; Datasets A–C are not required to include cross-page cases.
  Record expected results for every dataset/subset, using D19's precise metric definitions (miss rate, inaccurate-box rate, false-positive rate, duplicate/over-segmentation rate, merge-error rate for whole-page segmentation).
- [ ] 6.3 Execute the AI Provider Selection Gate against Qwen-VL and Kimi for each of the **three formally-gated contract/mode items** — whole-page question segmentation's `full_page` mode on PDF-render input, knowledge-tag labeling, and question-type classification — recording task correctness, structured-JSON success rate, latency, API cost, and failure/retry behavior; for whole-page segmentation specifically, execute the Gate's pass/fail judgment (miss rate/inaccurate-box rate/false-positive/duplicate/merge-error thresholds, plus the frozen "normal-clear-sample" eligibility definition and per-sample crash rule, per D19) only against Dataset B's digital_native held-out subset. Separately, and **NOT** counting toward this Gate's pass/fail for any candidate: (a) run the same `full_page`+PDF-render contract against Dataset B's scanned and mixed-or-ambiguous subsets (exploratory, same metrics, purely observational); (b) run `full_page` on grading-photo input (Dataset C, exploratory, per P1(4)/D15) recording the same five metrics; (c) run `locality_retry` mode (either input source) as a distinct repair-pipeline evaluation, recording its own repair-trigger success rate, never merged into `full_page`'s Gate score; (d) evaluate region localization as the repair-support track — record repair success rate (fraction of known-missing-number cases where a usable box was produced) and failure behavior; region localization still SHALL pass the same official-support/outbound-reachability/structured-output fail-closed checks below, but its own accuracy no longer determines Gate pass/fail; (e) evaluate error-question recognition's non-blocking pre-highlight track using **human-labeled ground truth on grading photos** (which questions are actually wrong) — never real parent behavior, which does not exist at development time — recording false pre-highlight rate, correct pre-highlight recall/coverage, unavailable/abstention rate, structured-output success, and latency/cost/failure-retry; the production-only "parent rejection rate of AI pre-selection" metric is a separate post-launch observability signal and SHALL NOT substitute for or be merged into this offline evaluation; (f) evaluate error-cause analysis per its existing dedicated non-blocking-track metrics (`clean_question_plus_candidate_evidence_region` / `photo_only_candidate_region` / `full_photo_fallback`) as defined in D17 — the rate of confidently-wrong suggestions, correct-suggestion coverage rate, the rate of incorrectly-judged-unavailable cases, final unavailable rate by reason, explanation faithfulness (no fabricated steps), and structured-output success rate, plus per-mode coverage/latency/cost/failure-retry behavior; reference causes for (e) and (f) SHALL be authored by a human reviewing the actual question and actual work, never derived from the model's own confidence score. None of (a)–(f)'s results affect Gate pass/fail for any candidate on the three formally-gated items. For every candidate across all of the above, verify it is officially supported for, and actually reachable from, the current Application Runtime's actual outbound source — currently the home-broadband mainland egress IP per D8, a deployment-stage condition, not a permanent architectural assumption; re-verify whenever the outbound source changes (e.g. after a future region migration). "Actually reachable" SHALL be established by issuing a real minimal API request through that outbound path, not inferred from documentation alone. For each candidate, record in the D13 decision record: the verification date; the official-documentation evidence/link consulted; the actual outbound environment used for testing; and the actual API reachability result. Fail-closed: a candidate SHALL NOT pass the Gate and SHALL NOT be selected as any contract/mode's provider if official support is absent, the candidate is unreachable from the actual outbound source, or the official support scope is ambiguous/unconfirmable. Additionally: execute PDF-type detector evaluation (Dataset A) independently of the three formally-gated items above (detection per D19 is not an AI Task Contract and is not subject to this Gate's Provider selection) — record the confusion matrix between human-labeled ground truth and detector output at the frozen `detectionVersion`, with the scanned/mixed-or-ambiguous-truth → digital_native-output cell tracked and reported separately as the highest-risk misclassification; freeze `detectionVersion` and its thresholds before running the held-out subset, and do not re-tune thresholds within the same version based on held-out results — a needed adjustment produces a new `detectionVersion` and repeats calibration → freeze → held-out. Dataset D (cross-page/out-of-scope) is reported separately as stress-test observations only, excluded from every dataset's pass/fail-relevant scoring.
- [ ] 6.4 Implement the AI Integration adapter(s) for the selected model(s) behind the six Task Contracts, including explicit empty/failure-result handling at the contract boundary; verify no provider-specific type leaks into Domain code, a simulated model failure yields each contract's defined empty/failure result (not an exception that crashes the caller), and specifically: whole-page question segmentation's two invocation modes return distinct, discriminated response shapes (D20) — `full_page` returns `{mode: "full_page", candidates: QuestionBoxCandidate[]}` (possibly empty), `locality_retry` returns `{mode: "locality_retry", candidate: LocalRepairCandidate | null}` (zero or one) — implementers SHALL NOT collapse these into one undifferentiated list type; an empty `full_page` list or low-reliability result triggers the explicit failure semantics defined in D15/grading-workflow spec (per-question "未解决" marking or whole-photo "unavailable" marking), never a silent skip or an unreliable box; region localization's adapter, now invoked only in the repair role (D20), keeps its existing single-target-number → single-box shape; on failure it SHALL NOT offer a free-hand manual box-draw fallback (removed — a manual-draw escape hatch here would directly contradict decision 2's literal "不是自由画框" wording and reintroduce the exact risk decision 2 exists to prevent) — a region-localization repair failure falls through to `locality_retry` (D20's dispatch step 5), and if that also fails the question is marked "未解决" per D15/grading-workflow spec, never blocking the rest of the batch; error-question recognition's adapter failure (empty/failed result) simply yields no pre-highlight — it no longer triggers any manual-list-entry fallback UI, since its output was never authoritative
- [ ] 6.5 Implement the "whole-page question segmentation" Task Contract adapter for **both input sources** (grading photo, per the no-PDF path; and confirmed PDF page render, per the PDF-backed path — P1(11)/D20 resolves the prior deferral) **and both invocation modes**: `full_page` (input: image only; output: `QuestionBoxCandidate[]`, possibly empty, no target-question-number input required) and `locality_retry` (input: image + `approximatePoint`/`regionHint`; output: zero-or-one `LocalRepairCandidate`); verify the two modes' response shapes are discriminated and never conflated, and no provider-specific type leaks into Domain code
- [ ] 6.6 Implement the error-cause analysis Task Contract adapter (D5/D17): first run the input pre-check entirely within the Application Runtime or the self-hosted Python Processing Service (D11) — never by calling any third-party AI Provider — covering blur/unreadability, occlusion/missing marks, (for `full_photo_fallback`) target-question locatability within the whole photo (may reuse D16's registration-reliability signal), and joint question+answer sufficiency; if the pre-check fails, return `unavailable` without calling the Provider and without sending any image, crop, or text content to it. If the pre-check passes, call the Provider with only the minimal necessary input for that input mode (never the full original photo or full PDF page when a smaller crop already suffices; the full photo is sent only under `full_photo_fallback` and only after the pre-check confirms locatability); return `primaryCategory` (one of the taxonomy v1 codes, tagged with the current `taxonomyVersion`) + `explanation` + an internally-calibrated confidence signal, persisting provenance as distinct fields: `providerModelIdentifier`, `contractVersion`, `taxonomyVersion`, `inputMode`, `generatedAt`, `confidence`, `primaryCategory`, `explanation`. Verify: (a) a pre-check failure never reaches the Provider and never sends any data to it; (b) `category=other` always carries a non-empty explanation; (c) `unavailable` never carries a category; (d) the minimal-input rule holds for all three modes; (e) all provenance fields are recorded distinctly, not merged into one opaque version string.
- [ ] 6.7 Calibrate the suggested/unavailable confidence threshold independently for each of the three error-cause input modes (D13/D17 non-blocking track), using human-labeled evaluation samples — a person reviews the actual question and actual work and records the reference error cause; a candidate's raw self-reported confidence (if any) SHALL NOT be treated as automatically trustworthy or used as its own ground truth. Record, per input mode: the rate of confidently-wrong suggestions (a suggestion shown as reliable but disagreeing with the human-labeled reference — the most dangerous failure mode); the correct-suggestion coverage rate; the rate of cases incorrectly judged `unavailable` when a correct suggestion was in fact achievable; the final `unavailable` rate broken down by reason (pre-check / below-threshold confidence / retry-exhausted). If a candidate Provider does not expose a calibratable confidence signal, the Application MAY construct its own reliability determination from evaluation results (e.g. output consistency across repeated calls), but SHALL NOT fabricate a falsely-precise probability. Do not invent threshold values now — establish and configure them from actual calibration results, separately per input mode; do not share one threshold across modes. Verify: whenever a mode's threshold has not yet been established, or calibration shows the signal to be unreliable, the contract returns `unavailable` for that mode rather than a bad/misleading category.

## 7. Grading Workflow — Build and Wire the Normal & Exception Entry Points

Builds the real AI-assisted identification/localization UI net-new for the PDF-backed path (Group 4 provides no equivalent to extend); the no-PDF path (7.5) and page-picker (7.4) continue to extend Group 4's existing page-confirmation/fixture pieces where applicable.

- [ ] 7.1 Build and wire the PDF-backed `full_page` click-to-select UI (D20/grading-workflow spec): once a PDF page is confirmed (by any means — 4.4's manual confirm, or 7.4's AI-assisted confirm), call the whole-page question segmentation Task Contract's `full_page` mode (6.4/6.5) against the confirmed PDF page render; render the returned `QuestionBoxCandidate[]` as clickable boxes; clicking toggles a box's selected/deselected state; the parent's explicit confirmation of the selected set persists it as the confirmed business fact (D6). This is the product's first user-facing entry point for PDF-backed error-question identification/localization — nothing in Group 4 provides an equivalent. An empty or unreliable `full_page` result is NOT a batch-ending terminal state: the UI SHALL explicitly show "整页自动分割不可用" and SHALL NOT silently fall back to 4.3's fixture, a text list, or manual drawing — the parent remains able to build up the error-question set entirely via 7.3's click-approximate-position recovery, even starting from zero boxes. Verify: (a) segmentation never runs against an unconfirmed page; (b) an empty `full_page` result renders the explicit "整页自动分割不可用" state while still allowing per-click recovery via 7.3, never a silent empty screen or a fallback to 4.3's fixture; (c) a question successfully recovered via 7.3 after an empty `full_page` result can be confirmed and saved normally; (d) one question's unresolved status (after exhausting repair) does not block confirming or saving other questions in the same session; (e) the confirmed selected set is what 8.1 later reads — no other mechanism (4.3's fixture, a text list) can produce it in a shipped build.
- [ ] 7.2 Wire AI pre-highlight into the 7.1 click-select UI (D20/grading-workflow spec): run error-question recognition (6.4) against the grading photo, `QuestionBoxNumberAssociation` (3.7) against the `full_page` result, and the page-level pre-confirmation reliability signal (3.8); when a candidate question number is `reliable` and uniquely associated with a box, render that box as "AI 建议选中" (non-assertive), pre-checked but not confirmed. Verify: (a) a `reliable`+unique association renders the pre-highlight; (b) any non-`reliable`/ambiguous/failed association renders no pre-highlight for that box, falling through silently to plain click-select — never blocking, never reverting to a text-list entry; (c) when 3.8's page-level signal is below its calibrated threshold, no pre-highlight is shown for the entire session even if individual associations would otherwise qualify; (d) the parent's explicit confirmation, not the pre-highlight state, is what persists as the confirmed set.
- [ ] 7.3 Wire the D20 repair-dispatch chain into the 7.1 UI's click-approximate-position gesture: on a parent's click at a position with no matching box, check whether a uniquely `reliable`, currently-unmatched question number exists AND is spatially compatible with the click (per D20's dispatch steps 1–2, using 3.7's `evidence` and 3.8's signal); if both hold, call region localization (6.4) for that number first; otherwise, or if region localization fails/is unavailable, call `locality_retry` (6.5) at that position. Either mechanism's result is offered only as a candidate requiring explicit parent confirmation. If both mechanisms fail for a given question, mark it "未解决" (no ErrorQuestion created), leave already-confirmed questions in the same session unaffected, and surface reason-specific recovery options per D20/grading-workflow spec: retry (always offered), reselect the confirmed PDF page (offered when page correspondence may be wrong), and replace/re-upload the grading photo (offered only when the issue concerns photo quality/matching or pre-highlight/evidence extraction — never presented as a fix for the PDF `full_page`/`locality_retry` segmentation itself, since the PDF render is a deterministic input a re-upload cannot change). None of these SHALL offer a from-scratch free-hand box-draw entry point. Verify: (a) a uniquely-known, spatially-compatible number triggers region localization first; (b) a known number that is NOT spatially compatible with the click, or no uniquely-known number, goes straight to `locality_retry`; (c) exhausting both never produces a manual-draw entry point, only the "未解决" terminal state; (d) a "未解决" question does not block confirming/saving the rest of the session's already-selected questions; (e) the "更换/重拍批改照片" option is never presented as fixing a pure segmentation/repair failure — only page-reselect and retry are offered for that case, with replace-photo appearing only when the underlying issue is photo-quality/matching-related.
- [ ] 7.4 Wire AI page-recommendation candidates into the page-picker built in 4.4 for multi-page PDFs (D14/grading-workflow spec): when a reliable candidate exists (per the D13 Selection Gate / P1(9) reliability criteria — treat as no-candidate whenever that criteria is not yet established or not met), pre-select it and require the parent's explicit confirm/override action before the mapping is persisted as confirmed — the pre-selection itself must never be treated as confirmed or passed to downstream steps (region localization, worksheet generation) until confirmed; when no reliable candidate exists, fall back to the full manual list from 4.4. This task is an enhancement that reduces page-selection effort — it is NOT a precondition for the D20 `full_page` normal entry (7.1), which depends only on "a page has been confirmed via D14" by any means (single-page auto-confirm, 4.4's manual list, or this task's AI pre-selection+confirm). Verify: (a) an unconfirmed AI pre-selection is never consumed by downstream steps; (b) a manual override persists in place of the AI suggestion; (c) confirmed mappings are not overwritten by later AI recompute; (d) with no AI page candidate available (or this task not yet implemented), a page confirmed manually via 4.4 still allows 7.1's `full_page` task to run normally; (e) `full_page` segmentation never runs against an unconfirmed page.
- [ ] 7.5 Wire the whole-page question segmentation Task Contract's photo-input output (6.5) into a click-to-select error UI for the no-PDF path (D15/grading-workflow spec): render returned boxes as clickable regions; clicking confirms an error question; clicking an approximate location for a missed question triggers a re-segmentation/fill-in retry. Verify: (a) a question that still cannot be reliably segmented after retry is marked "未解决" in the GradingSession, no ErrorQuestion is created for it, and the parent can re-photograph/replace the photo or explicitly abandon that question; (b) other reliably-segmented and confirmed error questions in the same session are unaffected and still save normally; (c) when the whole photo produces no reliable boxes at all, or the overall segmentation result is unusable, the entire photo is marked unavailable and the parent is prompted to re-photograph or replace it; (d) neither failure path silently skips, generates an unreliable box, or offers a manual free-hand box-drawing tool. (This task's box-rendering/toggle presentational component is shared with 7.1/7.3's PDF-backed equivalents — only the box-render/select/toggle/status-display behavior is shared. This task's whole-page-failure orchestration (re-photograph/replace prompt) is specific to the photo-input path and SHALL NOT be conflated with the PDF-backed path's whole-page-failure orchestration in 7.1/7.3, which degrades to per-click `locality_retry` recovery instead of an asset-replacement prompt, since re-rendering the same PDF fixes nothing a photo retake would.)
- [ ] 7.6 Verify that re-triggering `full_page` segmentation, pre-highlight association, or the D20 repair-dispatch chain for a GradingSession that already has a confirmed box set does not modify or delete the original grading-photo or PDF assets, and does not overwrite the already-confirmed box set or any already-created ErrorQuestion (D6/D9/grading-workflow spec); simulate a recompute after confirmation and verify both the assets and the confirmed facts are unchanged.
- [ ] 7.7 Run and record an end-to-end walkthrough of the PDF-backed path's upload→identify→localize→crop→assemble pipeline, using one real sample worksheet + photo, entirely through the product's real UI with no dev-only fixture (4.3) or seeded data involved: upload, D14 page confirmation (manual or AI-assisted), `full_page` segmentation, optional pre-highlight (7.2), parent click-select confirmation (with 7.3's repair dispatch exercised if needed), clean-question extraction (4.5), and correction-worksheet assembly with the default answer space (4.6 — type-classification wiring is 9.1's separate task, not yet exercised here). This supersedes 4.7's pipeline-only walkthrough as the first true real-UI proof of the normal entry point. This task SHALL NOT be described or reported as validating Question Bank persistence/knowledge-tagging (Group 8), redo tracking (Group 8), or Generation Snapshot re-fetch (Group 9's later tasks) — those remain separately verified by their own Groups. Verify the walkthrough produces a correct correction worksheet using only Groups 1–7 and 4.6's already-implemented pipeline, with no fixture involved.

## 8. Question Bank

- [ ] 8.1 Implement the step that commits a GradingSession's confirmed error questions — for the PDF-backed path, the parent-confirmed box set produced by Group 7 (7.1's click-select confirmation, as layered by 7.2's pre-highlight and 7.3's repair dispatch — both of which only ever feed candidates into that same single authoritative confirmed set, never a separate one); for the no-PDF path, the parent-confirmed box set from 7.5 — into permanent Question Bank `ErrorQuestion` records — source worksheet, error timestamp, confirmed region info, grading-photo reference, and the Child inherited from the GradingSession's childId as bound at that session's creation (D4/child-profile spec/question-bank spec). This commit step SHALL NOT read `currentChildId`, and SHALL NOT independently derive or re-derive the Child — it only persists the value already established by the GradingSession (the binding rule itself is implemented and verified by 5.10; this task verifies the Question Bank commit path actually consumes and persists that value correctly). This commit step SHALL NOT consume 4.3's dev-only fixture data in a production code path, SHALL NOT treat an AI pre-highlight (7.2) as itself sufficient confirmation, and SHALL NOT treat an unconfirmed repair candidate (7.3, region localization or `locality_retry` output) as part of the committed set until the parent has explicitly confirmed it. Verify: (a) all fields, including the Child, are saved once the workflow completes; (b) if `currentChildId` has changed at any point after the GradingSession's creation — regardless of whether it has since switched back to the original value by the time of commit — the committed `ErrorQuestion` still resolves to the Child bound to its `GradingSession` at creation; (c) the commit path never queries `currentChildId` to determine the Child it writes; (d) if the GradingSession's childId is missing, invalid, or cannot be resolved to a Child owned by the requesting User, the commit SHALL fail rather than falling back to `currentChildId` or writing an `ErrorQuestion` with ambiguous ownership; (e) a pre-highlighted-but-not-explicitly-confirmed box, or an unconfirmed repair candidate, never produces a committed `ErrorQuestion`; (f) no committed `ErrorQuestion` in a production-configured run can be traced back to 4.3's fixture data
- [ ] 8.2 Implement AI knowledge-tag generation as a draft with a human confirm/edit step, non-blocking on AI failure (empty tag, manual fill allowed); verify the error question still saves successfully when AI tagging fails
- [ ] 8.3 Implement provenance tracking so a human-edited knowledge tag is never overwritten by a later AI recompute (D4); verify a triggered recompute does not alter an already human-edited tag
- [ ] 8.4 Implement the two independent region fields on ErrorQuestion — clean-question region (from PDF, may be absent) and answer-evidence region (from grading photo) (D4/question-bank spec), plus the "缺少干净原题" marker derived from clean-question-region absence. Verify: (a) an absent clean-question region is stored as absent, never backfilled with the answer-evidence region's content; (b) the ErrorQuestion is marked "缺少干净原题" whenever the clean-question region is absent, and this marking is persisted; (c) the marking is visible in the question-bank list view; (d) the marking is visible in the question-bank detail view; (e) the answer-evidence region's `kind` ("候选上下文区域" or "整照片兜底", D16) is persisted explicitly and is never inferred from an empty/null bbox field
- [ ] 8.5 Implement AI error-cause suggestion generation as an asynchronous, resumable follow-up to the error-question save flow (question-bank spec/D17) — NOT synchronously awaited as part of that save transaction: the `ErrorQuestion` record persists independently and completely first; error-cause analysis (6.6's contract call, including its pre-check) is then triggered as a separate task via a durable mechanism (e.g. durable job, transactional outbox, or periodic reconciliation — specific product left to implementation) that guarantees the trigger is not permanently lost even if the initial dispatch fails. Maintain an internal analysis state machine distinguishing at least: not started, in progress, a retryable/transient-failure state (network error, Provider timeout, rate limit, worker interruption — never immediately recorded as `unavailable`), final `unavailable` (only for: pre-check insufficient input / below-threshold confidence / retry policy exhausted), and `suggested`; the user-facing display may simplify these to "analyzing / temporarily unavailable / suggestion available". Each analysis attempt carries a stable idempotency identifier. Persist a `suggested` result as an immutable `aiSuggestionSnapshot` (6.6's provenance fields) only once. Verify: (a) Provider timeout, failure, queue failure, or prolonged unavailability never fails or blocks the error-question save transaction, and never makes viewing, redoing, or printing the error question wait on the analysis result; (b) a process crash, worker restart, or call timeout never leaves the analysis permanently stuck in "in progress" — it recovers via retry or reconciliation; (c) a transient error (network/timeout/rate-limit/worker-interruption) is recorded as retryable, never immediately as `unavailable`; (d) duplicate dispatch, retries, or concurrent execution never produce more than one `aiSuggestionSnapshot` for the same analysis; (e) a late or stale execution result never overwrites a newer snapshot and never alters an existing `confirmedCause`; (f) a generated `aiSuggestionSnapshot` is never overwritten in place by a later automatic reanalysis; (g) whether an active manual reanalysis entry point exists is out of scope — none is added this round. The specific queue/task-scheduling product is left to implementation.
- [ ] 8.6 Implement the human confirm/edit UI for error-cause suggestions, producing a separate `confirmedCause` record (`category`, `explanation` — always required, non-empty — `taxonomyVersion`, `confirmedAt`, and `sourceSuggestionSnapshotId` when the confirmation originates from confirming/editing an existing `aiSuggestionSnapshot`) distinct from `aiSuggestionSnapshot` (question-bank spec/D17): verify confirming/editing creates or updates `confirmedCause` without altering the original `aiSuggestionSnapshot`; verify `category` is validated against its declared `taxonomyVersion` and rejected if the code doesn't exist in that version; verify `explanation` cannot be empty regardless of category; verify once `confirmedCause` exists, no automatic reanalysis overwrites it; verify the error question saves successfully whether or not `confirmedCause` is ever created.
- [ ] 8.7 Implement taxonomy versioning for error-cause categories (D5/D17/question-bank spec): tag each `aiSuggestionSnapshot`/`confirmedCause` with the `taxonomyVersion` active at generation/confirmation time. The v1 taxonomy is exactly: `calculation_error`, `concept_error`, `problem_understanding_error`, `solution_method_error`, `other` (requires a non-empty explanation); `unavailable` is a status, never a category value. Verify: (a) at `taxonomyVersion` v1 only these five codes are accepted, no others; (b) a later taxonomy update does not retroactively reclassify or relabel existing historical records — they keep their original `taxonomyVersion` and category value unchanged; (c) existing category codes are never redefined to a different meaning across versions.
- [ ] 8.8 Implement the RedoAttempt data model and derivation logic for ErrorQuestion (D18/question-bank spec): each RedoAttempt has immutable `originalResult`, `completedAt`, `correctionWorksheetId` (optional), plus an always-present (possibly empty) append-only `correctionAudit[]` (`correctionRequestId`, `fromResult`, `toResult`, `correctedAt`); `effectiveResult` is a read-only projection — `originalResult` when `correctionAudit` is empty, otherwise the last audit entry's `toResult`. `currentReviewStatus` and `redoCount` are read-only projections over the RedoAttempt collection (`currentReviewStatus` from the latest RedoAttempt's `effectiveResult`; `redoCount` = collection size) — never independently initialized or updated as their own state. A submission carries a `redoSubmissionId`: repeated requests with the same id create at most one RedoAttempt; two genuine redo submissions with different ids — even with the same result — each create their own RedoAttempt. When `correctionWorksheetId` is provided, the server SHALL validate before creating the record that the referenced CorrectionWorksheet actually includes this ErrorQuestion, belongs to the same Child, and that the caller has access; reject otherwise, never trusting a client-supplied id. Any server-side cache of the projections SHALL be rebuildable from the RedoAttempt collection and SHALL NOT be the source of truth. Verify: (a) an ErrorQuestion starts with no RedoAttempt and projections "尚未检查"/0; (b) printing/re-fetching a worksheet or opening the detail view never creates a RedoAttempt or changes the projections; (c) repeated requests sharing the same `redoSubmissionId` create only one RedoAttempt; (d) two real submissions with different `redoSubmissionId`s but the same result create two separate RedoAttempt records and `redoCount` increases by two; (e) a submission whose `correctionWorksheetId` fails the containment/Child/access checks is rejected; (f) a rebuildable cache always matches the freshly-derived projection.
- [ ] 8.9 Implement the redo-result submission UI, reachable both from a generated CorrectionWorksheet's view and from an ErrorQuestion's question-bank detail view (D18/question-bank spec): for a PDF-backed ErrorQuestion, either entry point lets the user submit a redo result, optionally linking the CorrectionWorksheet via the worksheet entry point; for a photo-only ErrorQuestion, only the detail-view entry point is available, and the UI SHALL explicitly state "缺少干净原题，无法提供无历史痕迹的题目视图" rather than silently offering a degraded experience; the detail-view entry point works identically for both — submitting a redo result is never blocked by the absence of a clean question region or a generated worksheet. Verify: (a) a photo-only ErrorQuestion can have a redo result submitted from its detail view, and the UI shows the explicit "缺少干净原题" notice; (b) a worksheet-entry submission records that worksheet's id, a detail-view submission does not require it.
- [ ] 8.10 Implement the pre-redo display-timing choice and post-submission unhide (D18/question-bank spec): for a PDF-backed ErrorQuestion, before starting a redo, let the user choose "直接重做" (shows the clean question in-app, hides historical answer evidence and error-cause content by default, with an explicit reveal action) or "复习后重做" (shows history first). For a photo-only ErrorQuestion, the system SHALL NOT offer an in-app "直接重做" backed by a clean question view, and SHALL NOT crop or mask the handwriting to fabricate one; instead the UI offers "复习后重做" (view the existing photo/error-cause, then redo externally and submit) as the only in-app display option, alongside the plain submit-a-result path. After submission, unhide the historical content for viewing alongside this result — this is display unhiding, not a comparison of answer-taking processes: no new answer photo/content is captured. Verify: (a) PDF-backed "直接重做" shows the clean question and hides history by default, revealable on demand; (b) PDF-backed "复习后重做" shows history upfront; (c) a photo-only ErrorQuestion never presents an in-app clean-question redo view, and the UI displays the "缺少干净原题" notice; (d) after submission, historical content becomes visible in all cases without being framed as a comparison of two answer attempts; (e) no new answer photo or content is captured or stored as part of submitting a redo result.
- [ ] 8.11 Implement correction of the most recent RedoAttempt's effective result via append-only correctionAudit entries (D18/question-bank spec): a correction request carries a `correctionRequestId` and an `expectedEffectiveResult` (or equivalent optimistic-concurrency version); the server SHALL apply the correction — appending a new `correctionAudit` entry (`correctionRequestId`, `fromResult`, `toResult`, `correctedAt`) — only if the RedoAttempt's current `effectiveResult` matches `expectedEffectiveResult`; otherwise reject and require the caller to refresh and retry, never silently reapplying a stale expectation. If the current `effectiveResult` already equals the requested `toResult`, treat as a successful no-op without appending a new entry — but this no-op rule does not substitute for `correctionRequestId`-based deduplication or the optimistic-concurrency check; all three apply together. A correction SHALL NOT modify `originalResult`, `completedAt`, `correctionWorksheetId`, or any other RedoAttempt; SHALL NOT create a new RedoAttempt (redoCount unchanged); SHALL NOT trigger error-cause analysis or alter `aiSuggestionSnapshot`/`confirmedCause`; SHALL NOT record which family member acted. Verify: (a) a repeated request sharing the same `correctionRequestId` appends at most one audit entry; (b) two genuine corrections on the same RedoAttempt append two distinct audit entries, never overwriting one field; (c) a stale correction request whose `expectedEffectiveResult` no longer matches the RedoAttempt's current `effectiveResult` (because a newer correction already applied) is rejected, not silently applied; (d) a request whose target already equals the current `effectiveResult` succeeds as a no-op without a new audit entry; (e) `redoCount` never changes due to correction; (f) `currentReviewStatus` re-derives from the corrected `effectiveResult`.

## 9. Correction Worksheet — Type Classification & Generation Snapshot

- [ ] 9.1 Wire the question-type classification Task Contract (6.4) into the answer-space sizing logic built in 4.6, replacing the "default only" behavior with type-matched sizing plus default fallback on AI failure (D5/D7); verify calculation questions receive a larger answer space, fill-in/multiple-choice questions receive a smaller answer space, and classification failure falls back to the default space without blocking generation
- [ ] 9.2 Implement Generation Snapshot persistence at generation time — confirmed error list/order, region coordinates, type-classification results or defaults (D7); verify the snapshot captures everything needed to deterministically reproduce the output
- [ ] 9.3 Implement re-fetch of a previously generated correction worksheet using the persisted Generation Snapshot and/or cached final PDF, without re-running AI recognition/classification or any human-confirmation steps; verify repeated retrieval produces semantically consistent output and does not require re-upload/re-confirmation
- [ ] 9.4 Implement regeneration of the derived clean-question image from the original PDF + confirmed region info when the derived image is unavailable (D3); verify regeneration succeeds without requiring re-upload or re-confirmation
- [ ] 9.5 Implement fail-closed rejection of CorrectionWorksheet generation when the selected ErrorQuestion set includes any ErrorQuestion marked "缺少干净原题" (correction-worksheet spec): verify generation is rejected — not silently filtered — and the response explicitly lists which selected ErrorQuestions lack a clean-question region, prompting removal and retry
- [ ] 9.6 Verify generated CorrectionWorksheet output never includes historical answer evidence or error-cause content (correction-worksheet spec/D18); verify a generated printable PDF contains no such content for any included error question

## 10. Data Protection & Backup

- [ ] 10.1 Configure OSS cross-account + cross-region replication as the independent backup/recovery path for original assets (D10); verify replicated objects appear in the destination account/bucket
- [ ] 10.2 Implement a scheduled `pg_dump` job meeting RPO ≤ 6h, uploading dumps to a dedicated OSS backup path separate from primary asset storage (D8); verify a scheduled run produces a dump object in OSS
- [ ] 10.3 Implement success/failure observability for the backup job (logging/alerting); verify a simulated failure (e.g., invalid OSS credential) is visibly reported, not silent
- [ ] 10.4 Implement a backup retention policy (prune dumps outside the retention window); verify old backups are removed and recent ones retained
- [ ] 10.5 Execute a restore test: restore the most recent backup into a clean PostgreSQL instance and verify key business data (confirmed error questions, region coordinates, knowledge tags) is present and correct; document the procedure as a runbook

## 11. Service Robustness Hardening

- [ ] 11.1 Implement request/job identity and idempotent retry handling between the Next.js adapter and the PDF Processing Service, with a bounded timeout on the Next.js side; verify a retried call does not produce duplicate or partially-applied output
- [ ] 11.2 Implement Next.js-side handling of PDF-service unavailability as an explicit failure state that never silently advances the workflow to "success" (D6/D11); verify a simulated service-down scenario surfaces a clear failure without corrupting already-confirmed business facts
- [ ] 11.3 Implement an input file size limit for the PDF Processing Service, rejecting oversized input explicitly rather than attempting it; verify an oversized input is rejected with a clear error

## 12. Production Deployment Readiness

- [ ] 12.1 Select and configure the production-grade public ingress mechanism (tunnel/reverse proxy) providing HTTPS access from mainland browsers to the Next.js container; verify a request from an external mainland network reaches the app
- [ ] 12.2 Investigate and document ICP filing / domestic web-service compliance requirements for the chosen ingress mechanism and mini PC hosting (D8 open question); record findings and any required action
- [ ] 12.3 Re-validate the already-implemented backup mechanism (10.2) under production network conditions — "mini PC → OSS backup upload" stability over the real production ingress/network path — plus "mainland browser → ingress → mini PC" latency/stability per the D8 Smoke Test Gate; this is re-validation of existing mechanisms, not a reimplementation; record results

## 13. Final Cross-Cutting Verification

- [ ] 13.1 Review Storage/Auth/AI/PDF Processing boundaries and confirm no Provider-specific SDK or type (OSS, PNVS, Email provider, AI model, PyMuPDF/HTTP client) leaks into Domain Model code (D3/D5/D9/D11/D12); document the review
- [ ] 13.2 Run a full end-to-end walkthrough on the production-deployed stack — upload archive, upload grading photos, `full_page` segmentation with click-select confirmation (optional pre-highlight; repair-dispatch recovery exercised if needed), save to question bank, generate and re-fetch a correction worksheet — using a real sample worksheet; verify the complete path works under production conditions with no dev-only fixture involved
- [ ] 13.3 Verify access-control end to end in production: unauthenticated requests are rejected across every protected route/API, including signed-URL issuance for assets (access-control spec); verify no business data is returned prior to authentication
- [ ] 13.4 Verify the Group 1.4 local-dev seed mechanism is disabled or unreachable in the production build/configuration, and cannot be used to bypass OTP/allowlist to create or access a User/Identity (D2/access-control); verify attempting to invoke it against a production configuration fails or is not present
- [ ] 13.5 Verify the Group 4.3 dev-only seed/fixture mechanism is disabled or unreachable in the production build/configuration, is never exposed through any UI route, uses clearly-marked dev/test data or an isolated dev/test database/schema never mixed into real User/Child/WorksheetArchive/GradingSession/ErrorQuestion data, and cannot be used to bypass the Group 7 normal/exception entry points to create or access ErrorQuestion data (D6/grading-workflow spec); verify attempting to invoke its seed/helper/route against a production configuration fails or is not present — mirrors the existing 13.4 check on 1.4
- [ ] 13.6 Verify D6's user/session interruption durability for a representative set of already-confirmed business facts and displayed-but-unconfirmed snapshots. This task covers only the user/session interruption types D6 lists (page refresh, browser close/reopen, logout→login, cross-browser/cross-device login) — it does NOT claim to cover D6's other operation types (AI recompute not overwriting confirmed facts, derived-asset regeneration not changing confirmed facts), which remain separately verified by their own existing tasks (e.g. 7.6/D20 for recompute, 8.3/8.6 for AI reanalysis not overwriting a confirmed knowledge tag/`confirmedCause`, 9.4 for derived-asset regeneration). It re-verifies existing persistence without reimplementing it, and reuses 5.8's already-established logout/login + fresh-device/session verification for `currentChildId` rather than redesigning or reimplementing it.

  Using one real GradingSession carried through to a saved ErrorQuestion and a generated CorrectionWorksheet, confirm the following representative facts: the photo↔PDF page correspondence (D14); the parent-confirmed authoritative question-box set (D20/7.1); the confirmed answer-evidence region or whole-photo-fallback marking (D16/4.10); the saved ErrorQuestion (8.1); a confirmed knowledge tag (8.2 produces/confirms it; 8.3 is its non-overwrite provenance guarantee, not the confirmation step itself); a `confirmedCause` (8.6); an `aiSuggestionSnapshot` that has been displayed but never confirmed (D6/D17); an existing RedoAttempt (8.8); and a generated CorrectionWorksheet's Generation Snapshot (9.2).

  For each of the four session-interruption conditions — (i) a page refresh; (ii) closing and reopening the browser; (iii) logging out and back in within the same browser; (iv) logging into the same family account from a different browser/device with no shared localStorage/sessionStorage/browser cache (not simulated by reusing browser state) — verify: (a) each of the above facts is still present and correct; (b) the `aiSuggestionSnapshot` recovered is the exact same snapshot/version originally displayed, not replaced by a newly-triggered analysis; (c) the CorrectionWorksheet can be re-fetched using its existing Generation Snapshot (9.2/9.3) without re-upload, re-recognition, or re-confirmation, and the re-fetched output is still determined by that original snapshot; (d) a GradingSession interrupted after some but not all confirmation steps resumes at exactly its last explicitly-confirmed checkpoint; (e) any candidate/pre-highlight/boundary-drag/form-input state that was not explicitly confirmed before the interruption is not required to be recovered — an implementation MAY discard it or MAY restore it as a non-authoritative draft, but either way it SHALL NOT be silently promoted to a confirmed fact or enter any downstream authoritative data on recovery; this task does not require cross-device draft synchronization
